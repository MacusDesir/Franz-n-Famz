<%= form_with(model: checkout, local: true) do |form| %>
  <% if checkout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(checkout.errors.count, "error") %> prohibited this checkout from being saved:</h2>

      <ul>
      <% checkout.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :item %>
    <%= form.text_field :item, id: :checkout_item %>
  </div>

  <div class="field">
    <%= form.label :time %>
    <%= form.number_field :time, id: :checkout_time %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
